#include <stdio.h>
int count_set(int n){
    int count=0;
    while(n>0) {
        if(n&1) {
            count++;
        }
        n=n>>1;
    }
    return count;
}
int main() {
	// your code goes here
	int n;
	scanf("%d",&n);
	printf("%d\n",count_set(n));
return 0;
}

